class ScoresController < ApplicationController
    #before_action :authenticate_user
    #methods for scores should be here
    def random
        #current_game = find_game
        #current_user.games.find(params[:game_id])

    end
end
